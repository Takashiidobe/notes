(ns clj.core
  (:gen-class))

(load "lists/list_last")
(load "lists/last_two")
(load "lists/kth")
(load "lists/list_length")
(load "lists/list_reverse")
(load "lists/list_palindrome")
(load "lists/list_flatten")
(load "lists/remove_consecutive_duplicates")
(load "lists/pack_consecutive_duplicates")

(defn -main
  "I do nothing."
  []
  (println "I don't really do anything."))
