(defn remove_consecutive_duplicates
  "Returns a list without consecutive duplicates"
  [lst]
  (dedupe lst))
(defn remove_consecutive_duplicates
  "remove all elements that are consecutive in a list"
  [lst]
  (dedupe lst))
